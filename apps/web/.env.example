# Opcional. Por defecto la app usa /api/json-server (proxy de Next → puerto 3001).
# Descomenta solo si quieres llamar al JSON Server directamente:
# NEXT_PUBLIC_JSON_SERVER_URL=http://127.0.0.1:3001
